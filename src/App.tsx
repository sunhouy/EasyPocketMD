import EditorLayout from './components/EditorLayout';

export default function App() {
  return (
    <div id="react-root-container" style={{ height: '100%' }}>
      <EditorLayout />
    </div>
  );
}
